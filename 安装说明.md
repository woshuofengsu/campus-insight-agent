# 社区先知 CommunityInsight Agent — 一键安装说明

> 适用：Windows 10 / 11 · Python 3.10+

## 三步启动

1. **解压**本安装包到任意目录（建议路径不含空格和特殊字符）
2. **双击 `install.bat`** — 自动创建虚拟环境并安装依赖（首次约 2-5 分钟，需联网）
3. **双击 `start.bat`** — 自动打开浏览器 `http://localhost:8501`

首次启动会自动创建数据库并写入演示数据（38 条诉求、19 条提案、14 条知识条目）和演示账号，无需手动初始化。

## 关于 API Key

安装完成后根目录会生成 `.env` 配置文件，其中涉及两个外部 API：

| 变量 | 用途 | 不填的影响 |
|------|------|-----------|
| `DEEPSEEK_API_KEY` | 大模型对话（核心功能） | 对话/Agent 功能不可用，看板与数据仍可浏览 |
| `HEFENG_API_KEY` | 和风天气实时数据 | 天气显示为模拟数据 |

- 申请 DeepSeek：https://platform.deepseek.com
- 申请和风天气：https://id.qweather.com
- **完全不想申请**：把 `.env` 里的 `OFFLINE_MODE` 设为 `true`，系统进入离线演示模式

## 演示账号

| 角色 | 账号 | 密码 |
|------|------|------|
| 居民 | demo_resident | 免密 |
| 网格员 | demo_grid | demo123 |

## 常见问题

- **端口被占用**：`start.bat` 会自动结束占用 8501 端口的旧进程，无需手动处理
- **安装失败**：确认网络能访问 pypi.org；可改用国内镜像：
  `pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`
- **浏览器没自动打开**：手动访问 http://localhost:8501
- **关闭服务**：关闭运行 `start.bat` 的命令行窗口即可停止
