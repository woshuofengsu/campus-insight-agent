# 社区先知 CommunityInsight · 运行说明（给主办方/演示环境）

> 这是一份"傻瓜式"运行说明。只要电脑有 Windows + Python 3.10+，**双击一个文件就能跑起来**，不需要敲任何命令。

---

## 一、这是什么

「社区先知」是一套面向社区/街道的"接诉即办"网格化多智能体平台，通过 9 个分工协作的 AI 角色，服务居民、网格员、老年人三类用户，实现报修、提案、通知、政策问答、健康咨询、天气预警、紧急求助等场景的智能化处理。

本压缩包包含完整可运行的项目（前端已构建好，无需 Node.js）。

---

## 二、环境要求

| 项目 | 要求 |
|---|---|
| 操作系统 | Windows 10 / 11（64 位） |
| Python | 3.10 或更高版本（推荐 3.12） |
| 浏览器 | Chrome / Edge / Firefox 任意现代浏览器 |
| 网络 | 核心功能不依赖外部 AI 服务（LLM 不可用时自动降级为规则链路）；AI 增强与真实天气需要联网 |

> **如果电脑还没装 Python**：去 https://www.python.org/downloads/ 下载安装，**安装时务必勾选 "Add Python to PATH"**，装完重启电脑。

---

## 三、最简启动方式（推荐，双击即可）

1. **解压**本压缩包到任意文件夹（路径中不要有中文以外的特殊字符）。
2. 进入解压后的文件夹，找到 **`启动演示.bat`**。
3. **双击 `启动演示.bat`**。
4. 脚本会自动完成：检查 Python → 自动安装依赖（首次约 1-3 分钟）→ 检查配置 → 启动服务 → 自动打开浏览器。
5. 浏览器自动打开 `http://localhost:8000`，看到登录页即启动成功。
6. **演示结束后，关闭那个黑色命令行窗口即可停止服务。**

> 如果浏览器打开后显示"无法访问"，请等 2-3 秒后按 **F5 刷新**（服务启动需要一点时间）。

---

## 四、手动启动方式（如果双击脚本遇到问题）

打开命令行（CMD 或 PowerShell），进入项目文件夹，依次执行：

```
pip install -r requirements-web.txt
python -m uvicorn api_web:app --host 127.0.0.1 --port 8000
```

然后浏览器手动打开 `http://localhost:8000`。

> 如果 `requirements-web.txt` 安装后仍有缺少依赖的报错，请执行 `pip install -r requirements.txt`（完整依赖集）。

---

## 五、演示账号（已内置，直接登录）

| 角色 | 账号 | 密码 | 说明 |
|---|---|---|---|
| 居民 | `demo_resident` | 免密登录 | 王阿姨，居民端（海淀小区） |
| 居民（第二社区） | `demo_resident_cy` | `demo123` | 李叔，朝阳试点社区——**用于演示"同一句话、属地不同、政策不同"** |
| 网格员 | `demo_grid` | `demo123` | 刘网格员，网格员端 |
| 老年人 | `demo_elderly` | 免密登录 | 张大爷，老年端（大字/语音/长按求助） |

登录页选择对应角色，点击"演示登录"即可，无需输入密码（网格员端需输入 demo123）。

---

## 六、推荐演示动线（约 5-8 分钟）

1. **居民端**：登录 `demo_resident` → 提交一条报修（看状态流转）→ 问一个政策问题（看回答下方的**知识库引用出处**与**适用地区**）。
2. **属地化对比（本项目最能打的一枪）**：用 `demo_resident` 问"我们社区高龄老人有什么补贴？"→ 命中**海淀区**级文件；
   换成 `demo_resident_cy / demo123` 问**同一句话** → 命中**北京市**级文件。**属地来自账号的网格归属，不采集定位**。
3. **王牌展示**：展示天气预警 → 健康防护建议 → 通知联动的**主动协作流程**，以及**黑板消息流**（9 个角色之间的真实消息，这是"真多智能体"的核心证据）。
4. **老年端**：登录 `demo_elderly` → 展示大字界面、语音报修入口、**长按紧急求助按钮**（防误触设计）。
5. **转人工**：展示机器判断不了时一键转人工，以及"人工处理包"（用户信息 + 完整执行链 + 待确认事项）。
6. **网格员端**：登录 `demo_grid/demo123` → 展示工作台、待办、AI 助手、统计导出。

---

## 七、常见问题

**Q1：双击 `启动演示.bat` 后提示"未找到 Python"？**
A：电脑没装 Python，或装的时候没勾选 "Add Python to PATH"。请安装 Python 3.10+ 并勾选 PATH，装完重启。

**Q2：依赖安装很慢或失败？**
A：脚本默认用清华镜像源，国内较快。如果失败会自动切换官方源。也可以手动执行 `pip install -r requirements-web.txt`。

**Q3：提示"8000 端口已被占用"？**
A：电脑上已有其他程序占用了 8000 端口。请关闭占用 8000 的程序（可能是之前启动的本项目未关），或修改启动命令中的端口号。

**Q4：AI 回答/真实天气不工作？**
A：这两个功能需要在 `.env` 文件中配置 `DEEPSEEK_API_KEY`（大模型）和 `HEFENG_API_KEY`（和风天气）。**不配置也完全不影响核心功能演示**——系统会自动回退到规则引擎，报修/提案/通知/政策检索/多智能体协作/适老化等核心场景全部正常。

**Q5：数据库里有数据吗？**
A：压缩包已内置演示数据库（`data/community_insight.db`），包含演示账号和样例数据，开箱即用。如需重置，删除该文件后重启服务会自动初始化。

**Q6：可以在其他电脑上访问吗？**
A：默认绑定 `127.0.0.1`（仅本机访问）。如需局域网其他设备访问，将启动命令改为 `python -m uvicorn api_web:app --host 0.0.0.0 --port 8000`，然后用 `http://电脑IP:8000` 访问。

---

## 八、技术概览（供技术人员参考）

- 后端：FastAPI（Python），主服务端口 8000
- 前端：Vue3 + Naive UI，已构建为静态文件（`web/dist`），由后端直接提供，无需 Node.js
- 数据库：SQLite（WAL 模式），schema 版本 v46，约 50 张业务表
- 多智能体：9 个角色，黑板协作（带锁/版本/历史），5 种消息协议，仲裁器（合规>安全>专业>人工>数据一致>保守），校验器，合规审计员一票否决
- LLM：DeepSeek，规则为主、LLM 为辅，4 个开关默认关，超时自动回落规则
- 安全：AES-256-GCM 敏感数据加密，展示/导出脱敏，参数化 SQL，最小权限
- 属地化：政策按行政区划**本地优先、全国兜底**（属地只影响排序，不改变"能否自动回答"）；**不采集定位**
- 工程：638 项 pytest（637 通过 + 1 项需外部服务跳过），550 并发零失败（演示登录端点；业务混合压测服务端 5xx=0），ruff 0 问题

---

## 九、联系

项目作者：步承泽
如遇运行问题，请联系作者协助。
