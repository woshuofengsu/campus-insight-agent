# 提交清单（照着勾即可）

> **项目**：社区先知 CommunityInsight Agent · 基层治理赛道（申报人文赋能特别奖） · 单人参赛（步承泽）
> **更新时间**：2026-09-14 · 对应代码库：schema v46 · 可运行用例 **638**（637 通过 / 1 需外部服务跳过）
> **形态**：可运行的**演示级原型（Demo）**，非已上线生产项目；**尚无真实社区试点与真实用户**（详见创意说明书 §5.1）

---

## 一、要交的文件（已就绪 ✅）

| # | 材料 | 交什么 | 位置 |
|---|---|---|---|
| 1 | **智能体创意说明书**（正式提交件） | `创意说明书-提交版.pdf` 或 `.md`/`.html` | `docs/competition/创意说明书-提交版.md`、`docs/competition/pdf/` |
| 2 | **诚信与知识产权合规声明** | `诚信声明-提交版.pdf`（需**手写签名 + 日期**） | `docs/competition/诚信声明-提交版.md`、`pdf/诚信声明-提交版.pdf` |
| 3 | 团队/个人信息表 | 已内嵌在创意说明书 §一（含身份证号、院校、职责） | 同上 |
| 4 | 技术实现报告（佐证材料） | `技术实现报告.pdf` | `docs/competition/技术实现报告.md` |
| 5 | 交付说明（怎么跑/怎么验） | `最终版交付说明.pdf` | `docs/competition/最终版交付说明.md` |
| 6 | 答辩问答手册（25 问） | `答辩问答手册.pdf` | `docs/competition/答辩问答手册.md` |
| 7 | 演示脚本 / 提词器 | `演示脚本.pdf` + `演示提词器.html` | `docs/competition/` |
| 8 | 五维自查报告（可选加分） | `评委评审报告.pdf` | `docs/competition/评委评审报告.md` |
| 9 | 源码仓库 | 分支 `fix/WS0-WS10-audit`（含 50 节开发日志） | GitHub |

**PDF 一键生成**（改了 md 就重跑，否则交的是旧版）：

```powershell
python scripts/export_pdf.py            # 自动先刷新 HTML 再导出 7 份 PDF → docs/competition/pdf/
python scripts/export_pdf.py 创意说明书-提交版   # 只导一份
```

---

## 二、必须人工完成的 4 件事（我这边做不了 ⚠️）

| # | 事项 | 怎么弄 | 说明 |
|---|---|---|---|
| 1 | **学生证扫描件** | 学生证照片/扫描件 | 创意说明书 §一 成员信息表里"学生证扫描件"列现为「**（附件待附）**」 |
| 2 | **原型界面截图** | 打开服务后三端各截 2–3 张（居民对话页 / 网格员工作台 / 老年关怀版）→ 插入附件或单列附件 | 附件材料第 2 条现为「原型界面截图（**附件待附**）」；已有录屏可用 `.recordings/` 抽帧 |
| 3 | **诚信声明签名** | 打印 → 签字 → 扫描或拍照 | `诚信声明-提交版.pdf` 末尾需手写签名与日期 |
| 4 | **报名系统表单** | 按系统字段逐项填 | **赛事名称/赛道/奖项名以官方通知原文为准**（材料里统一写作"基层治理赛道 / 申报人文赋能特别奖"） |

---

## 三、提交前最后 5 分钟自检（一条条跑）

```powershell
python scripts/serve_public.py --no-open     # 起服务（+公网隧道，手机可扫码）
python scripts/demo_preflight.py --fast     # 9 项自检，必须 9/9
python scripts/demo_acceptance.py           # 12 项端到端，必须全过
python scripts/check_claims.py              # 材料数字与代码一致
python -m pytest tests/ -q                  # 全量 637 passed / 1 skipped
python scripts/ui_audit.py                  # 54 页面视口 0 违规（约 10 分钟）
python scripts/mobile_audit.py              # 21 页移动端 0 违规
python scripts/export_pdf.py                # 重新导出 PDF（保证 PDF 不落后于 md）
```

**答辩前**：打开 `docs/competition/演示提词器.html`（键盘 1–7 切场景、空格自动播、← → 翻行）；
兜底录屏在 `.recordings/`（7 段，含属地化两社区对比 `07-region.webm`）。

---

## 四、提交给评委的现场入口

| 入口 | 地址 | 备注 |
|---|---|---|
| 本机 | `http://127.0.0.1:8000/login` | 演示主用 |
| 公网（评委手机扫码） | 跑 `python scripts/serve_public.py`，打开 `.shots/手机访问.html` 扫码 | **临时隧道无访问控制，演示完 `--stop`**；手机用 4G/5G |
| 演示账号 | 居民＝点「居民」免密 · 老年＝点「老年」免密 · 网格员＝`demo_grid` / `demo123` · 属地对比＝`demo_resident_cy` / `demo123` | 第二个账号用于"同一句话两个社区不同政策" |

---

## 五、口径纪律（答辩照此说，别越线）

| ✅ 可以说 | ❌ 不要说 |
|---|---|
| 演示级原型；机制有效、可审计、可复算 | "已在社区上线使用" |
| 属地来自账号网格归属，**不采定位** | "用了定位会更准" |
| 答不了就转人工，并给处理编号 | "准确率 100%" |
| 638 项用例（637 通过 / 1 跳过），6 项为供数冒烟、其余硬断言 | "测试全绿所以没有 BUG" |
| PWA 只做到"可添加到主屏幕" | "支持离线/断网可用" |
| 尚无真实试点，治理成效未验证 | 任何编造的成效/效率数字 |

---

## 六、提交后

```powershell
python scripts/serve_public.py --stop      # 关公网入口（链接立即失效）
# 需要连服务一起关：python scripts/serve_public.py --stop --all
```

> 本轮终局核验结论：13 项门禁全绿、静态审查零缺陷，详见 `docs/review/全项目终局核验报告.md`。
> 唯一还能实质提升说服力的动作：**拿一台真机（iOS Safari / 微信）走一遍老年端**，
> 把"未验证项"从 4 条减到 3 条。
