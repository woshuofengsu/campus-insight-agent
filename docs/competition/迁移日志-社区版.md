# 社区版迁移工作日志（海淀小区 · 接诉即办）

> 目标：把「校园先知 CampusInsight」完整迁移为「社区先知 CommunityInsight」，落地北京海淀区「海淀小区」接诉即办场景。架构与 Agent 内核不变，只换场景皮 + 数据 + 叙事，并做 UI 升级。

## 关键命名对齐（全局统一）

| 项 | 校园版 | 社区版 |
|----|--------|--------|
| 名称 | 校园先知 CampusInsight | 社区先知 CommunityInsight |
| 落地 | 北京工商大学 / 良乡校区 | 北京海淀区 / 海淀小区 |
| 双角色 | 学生 / 教师 | 居民 / 网格员 |
| 知 | 校园脉搏 | 社区脉搏 |
| 报 | 随手报修 | 接诉即办 |
| 议 | 有话说 | 邻里议事 |
| 督 | 治理透明窗 | 社区治理看板 |

**社区诉求分类（8 类，替换原校园 7 类）：**
设施维修 / 环境卫生 / 安全隐患 / 停车管理 / 噪音扰民 / 物业服务 / 邻里矛盾 / 社区事务

> ⚠️ 这 8 类需在 tools/action_report_issue.py（_CATEGORIES）、UI 枚举、文档中保持完全一致。

---

## 2026-08-13

### 迁移策略（贯穿全程的关键决策）

**保留内部标识符，只改用户可见文案。** 具体保留：表名 `campus_issues`、字段名 `school/grade/major/student_id`、role 值 `student/teacher`、工具函数名（`get_campus_pulse`、`query_issues`、`get_school_policy`、`report_issue` 等，它们在 prompt.py/enforce.py/fallback.py/offline_agent.py/reflector 中被硬编码）、`/api/campus-pulse` 路由路径、`CAMPUS_CITY` 等配置键名。这样不会破坏「真 Agent」的 DB 写链与工具调用链。

### 已完成 ✅

- **Phase 1 数据层：重写 data/seed.py 为社区版**
  - 38 条社区诉求 + 19 条邻里议事提案 + 14 条社区知识 + 7 议事话题 + 28 居民意见 + 28 反馈
  - 双角色：王阿姨（居民 `demo_student`）/ 刘网格员（网格员 `demo_teacher`，密码 `demo123`）

- **Phase 2 工具层：16 工具描述改写 + 分类关键词**
  - `tools/action_report_issue.py`：`_CATEGORIES` 重写为 8 类社区关键词；`_ROOM_REQUIRED_KEYWORDS`/`_ROOM_EXEMPT_KEYWORDS` 改社区地点
  - `query_campus_issues.py`（分类标签+emoji）、`query_campus_pulse.py`、`query_knowledge.py`、`query_proposals.py`、`action_create_proposal.py`、`action_support_proposal.py`、`action_express_opinion.py`（匿名学生→匿名居民）、`query_topics.py`、`query_my_issues.py` 全部迁移

- **Phase 3 Agent 层：System Prompt + 意图关键词 + offline**
  - `agent/prompt.py`：报修/议事/观察员关键词组全部换社区词，System Prompt 正文 校园先知→社区先知、例子 教三楼→3号楼
  - `agent/offline_agent.py`、`helpers.py`（地点正则）、`fallback.py`、`reflector/*`、`rag.py`、`engine.py`、`weekly_report.py` 全部迁移
  - 迁移后 tools/ 与 agent/ 目录经 grep 验证零校园语义残留

- **Phase 5a 配置层：config.py + .env.example + api.py**
  - 品牌 CampusInsight→CommunityInsight、校园治理平台→社区治理平台
  - api.py 的 FastAPI title/description、tags、docstring、OpenAPI 描述、演示账号注释全部迁移

### 进行中 ⏳（已派并行 sub-agent）

- **Phase 4 UI 层**（3 个 sub-agent）：居民端 9 页 / 网格员 6 页 / 组件入口（sidebar、login、onboarding、demo_tour 等）
- **Phase 5b 数据/感知层**（1 个 sub-agent）：`db_governance.py`、`db_perception.py`、`db_health_alerts.py`（健康密度模型换社区场景）、`db_notifications.py`、`db_core.py`、`live_generator.py`（演示数据整体重写）、`perception/monitor.py`
- **Phase 6 文档**（1 个 sub-agent）：创意说明书 + 技术实现报告 + 演示脚本/旁白 + README
- **Phase 7 测试**（1 个 sub-agent）：同步 test_tools/test_prompt/test_helpers/test_offline_agent/test_governance_audit/test_db_roundtrip/e2e/ablation/critical_paths/verify_all 的旧分类与旧 persona 断言

### 待办

- **审查优化 + 运行测试（至少累计 1 小时）**：全量 pytest + 手工冒烟 + 竞品级 UI 走查
