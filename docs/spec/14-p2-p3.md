# 14-P2/P3 优化项落地（适配版）

> 依据 P2 七项 + P3 五项完善版设计落地，并按本项目适配（零第三方依赖、规则引擎为主、不破坏存量）。

## P2 批次（已全部落地）

| 编号 | 方案 | 落地 |
|---|---|---|
| P2-06 | Prompt 注入防护 | `agent/prompt_guard.py` 输入过滤（指令覆盖/提示词泄露/角色扮演/数据泄露/越权/安全绕过 6 类）+ 固定安全语 + 留痕；Verifier `InjectionOutputRule` 输出检测；`agent/prompt.py` 系统提示加固（最重要规则 + 边界）+ 后端权限强制（既有） |
| P2-05 | LLM 成本控制 | v33 `llm_usage` 表 + `data/db_llm_usage.py`（记账/缓存命中/汇总/趋势）+ `engine.py` 两调用点记账 + `GET /agent/llm-usage` + grid 助手「查用量」；Web 端规则引擎零 LLM 即降本验证 |
| P2-07 | 引用强制 | Verifier `CitationRequiredRule`（政策无引用不回答）+ policy_expert 自动附「📎 参考：《标题》」+ 测试（既有，本轮补测） |
| P2-01 | 跨部门仲裁 | `roles/config.py` 部门优先级表 `DEPT_PRIORITY`（合规 1 票否决→安全→专业→费用人工→默认人工）+ 部门权限表 `DEPT_SCOPE`；arbiter 5 规则覆盖（费用/隐私/天气应急） |
| P2-02 | 重复上报合并 | `db_repair.find_duplicate_issue`（7 天内同楼栋 + 双字特征重合 ≥2，排除自身）+ 创建后检测 → `merged/original_id` + 通知原报修人「有新居民上报」+ 新报修人「已合并」 |
| P2-04 | 分层瘦身 | 适配决策：完整拆分 api_web（2400+ 行）风险高，本轮落地「服务层抽取」——新增独立模块 `data/db_llm_usage.py`/`db_opinion.py`/`agent/analytics.py`/`verifier.py`/`arbiter.py`/`prompt_guard.py`；api_web 保持路由入口。完整 routes 分包列为后续（评审 P2 项） |
| P2-03 | 语义聚类预测 | `agent/analytics.py`：TF-IDF 双字特征聚类（高频主题+环比）+ 周趋势 + `build_data_brief` 数据简报 + `GET /agent/analytics`；降级：数据不足只展示统计 |

## P3 批次（核心项落地，两项文档化）

| 编号 | 方案 | 落地 |
|---|---|---|
| P3-01 | 舆情监测 | v34 `public_opinion` + `data/db_opinion.py`（关键词分级 红/橙/黄/蓝 + 录入 + 列表 + **一键转工单**（红色/橙色自动紧急）+ 简报）+ 4 端点；外部 API（12345/媒体）接入点留 `auto_ingest` 占位（需申请接口权限） |
| P3-03 | 数据驾驶舱 | 复用 `Screen.vue`（治理大屏 30s 刷新）+ 新增 `build_data_brief` 数据叙事（趋势/异常/机会简报）+ `GET /agent/analytics` 供前端 |
| P3-04 | 异常屏幕 | `web/src/components/ErrorState.vue`（网络/服务/无权限/空数据 4 态友好屏）+ `/stability` 演示安全屏（质量基线 + 模拟异常按钮）；老年端异常提示沿用大字/语音 |
| P3-05 | CI/CD | `.github/workflows/ci.yml`：pytest 全量 + npm build + 冒烟（uvicorn 起服务跑 smoke_test）；Dockerfile.web 已有 |
| P3-02 | 多租户 | 适配决策：tenant_id 全表迁移会破坏 399+ 存量测试且单社区演示无收益——本轮**schema 预留 + 文档**（`community` 字段已存在可按社区过滤；多租户列为规模化阶段改造） |

## 验证
- `tests/test_agent.py` 33 项（新增：注入拦截/输出注入/用量统计/部门仲裁/重复合并/聚类/舆情全流程）+ security 8 + api_web 21
- 全量 pytest **399 passed**；构建通过；压测保持零失败
- 本轮提交：b0780b7（P2-06+SL A）、138357b（P2-05）、0624646+e10d70f+0d25987（P2-01/02）
