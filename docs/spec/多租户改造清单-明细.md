# 附：多租户真隔离 · 详细改造清单（源自只读侦察，file:line 已逐个读过）

> 本节是 `docs/spec/多租户与老年端深化-侦察报告.md` 的配套明细，供 B5 实施时直接照做。
> **未改任何文件**（侦察阶段）。

## A. 两个必须先知道的事实（决定方案成不成立）

| 事实 | 证据 |
|---|---|
| `DEFAULT_TENANT` 落的是**行政区**、不是社区名 | `config.py:73` → `COMMUNITY_DISTRICT`（默认 `"海淀区"`，`:71`）；实测 `config.DEFAULT_TENANT == '海淀区'` |
| **v41 只回填、从不写入**，租户值已劣化 | `db_core.py:673-686` 只有 `_add_column` + `UPDATE`。实测：`community_issues` 中 `'海淀区'` 224 行、**`''` 128 行**；`notices` **6/6 全空**。写入侧全都不带租户：`db_repair.py:186`、`db_proposal.py:254`、`db_notice.py:235`、`db_governance.py:108/453`、`seed.py:317/405/626`、`live_generator.py:225` |

**推论（最重要）**：只加列 + 回填的"迁移式隔离"必然失效——新数据全是空租户，隔离静默退化成"空租户"。
**写入侧必须与迁移同批改造**，否则就是 v41 的重演。

## B. 租户键 = 社区名（`user_profile.community`）

依据：`config.REGION_BY_COMMUNITY` 以社区名为键（`config.py:80-91`）；JWT 已带 `community`
（`deps.py:49-66`、`auth.py:56-57`）；老 token 有查库兜底（`deps.py:103-124`）；
通知可见范围已按社区算（`db_notice.py:173-182`、`:200-205`）。

**否决行政区当租户键的硬理由**：同区两个社区会互相看见。
**风险**：现存 `'海淀区'` 是区级值，v47 必须显式归一化；`DEFAULT_TENANT` 语义分裂
（`db_core.py:680` 在 fresh DB 上仍按它回填，改它会同时影响测试库与生产库，须一次定稿）。

## C. 核心表（P0 = 网格跨用户聚合读；P1 = 敏感数据）

**启用已有 `tenant_id`**：`community_issues`(352) / `proposals`(201) / `notices`(6，全空)。
**加列**：`health_consults`(180) / `policy_questions`(116) / `medication_reminders`(2) /
`emergency_contacts`(3) / `emergency_calls`(4) / `safety_reminders`(1) / `weather_check_tasks`(4) /
`agent_logs`(557) / `agent_dialogs`(1283) / `care_event_log`(48)。
**靠 `user_id` 派生、不加列**：`elderly_profile`(1)。
**需确认**：`activity_log`(15342) 只有 `actor`（名字、无 id）——建议按 `target_type/target_id` 派生。

## D. 查询点（按危险度排序）

| 危险度 | 位置 | 问题 | 改法 |
|---|---|---|---|
| **最高** | `api_routes/export.py:26` | `get_issues(limit=1000)` **全量 CSV 导出** | 传 tenant |
| **最高** | `api_routes/batch.py:56,69,82` | 批量派单/关闭/回复直接吃客户端 `issue_ids`/`question_ids`，**无归属校验（IDOR：可改他社区工单）** | 加归属校验 |
| 高 | `db_notice.py:197-198` `_notice_visible_to_user` | `"全体居民"` → **任何人可见**（跨社区当前必现泄漏） | 改为"本社区居民可见" |
| 高 | `api_routes/health.py:116` `/linkage/active` | 按 `module='疾病预防'` 读 `activity_log`，**无用户约束且居民端可访问** | 按租户过滤 |
| 高 | `db_notice.py:815/867` | 取全部已发布再 Python 过滤 | 加 `tenant_id=?` + 修可见性函数 |
| 高 | `db_policy.py:1506,1512` | `get_common_questions` 全库 `GROUP BY` 聚合（**居民端也暴露**） | 加 `tenant_id=?` |
| 中 | `api_routes/issues.py:150` + `db_repair.py:661` | grid 工单列表无租户 | 加 `tenant_id=?`（grid 必传） |
| 中 | `api_routes/proposals.py:121` + `db_proposal.py:1174/1268` | 提案列表/导出（`get_export_rows` 是批量外泄主口） | 透传 tenant |
| 中 | `db_agent.py:105/277`、`export.py:146` | agent 日志、自转率统计 | 加 `tenant_id=?` |
| 中 | `db_board.py:52,59,66,70,148`、`agent/analytics.py:27,32,61` | 红黑榜、聚类、周趋势全表聚合 | 加 `tenant_id=?` |
| 中 | `api_routes/health.py:310` + `db_health_content.py:941/1275` | 咨询列表与 CSV | 加租户过滤 |
| 中 | `api_routes/elderly.py:356/379/406/412` | 用药、紧急联系人、久未活跃、SOS 全库 | 传 tenant |
| 中 | `api_routes/weather.py:125` + `db_weather.py:833` | 天气检查任务 | 加 `tenant_id=?` |
| 中 | `db_care_metrics.py:50-59` | 关怀指标全库聚合 | 加 `tenant_id=?` |
| 中 | `db_repair.py:693` | `get_safety_reminders` | 加 `tenant_id=?` |
| 中 | `db_notice.py:744,748` | 已读统计全库 | 加租户口径 |

## E. v47 迁移设计（照现有 `_mN_*` 写法）

```python
def _m47_tenant_isolation(conn):
    """v47：核心表租户隔离。加列 → 归一化 → 按用户社区回填 → 建索引；可重复执行。"""
```
- **加列**：§C 的 10 张表各加 `tenant_id TEXT DEFAULT ''`（照 `db_core.py:618-622` 的 PRAGMA 幂等模式）。
- **归一化**（关键）：把区级值按**显式映射**改成社区名（`'海淀区'` → `'海淀小区'`），
  未命中项 `_log.warning` 计数，**不许静默**。
- **回填**：`UPDATE ... SET tenant_id=(SELECT community FROM user_profile u WHERE u.id=<reporter_id/user_id>)
  WHERE tenant_id='' OR tenant_id IS NULL`；无主的行落 `DEFAULT_TENANT` 并记数。
- **索引**：`CREATE INDEX IF NOT EXISTS idx_<table>_tenant ON <table>(tenant_id)`（照 `_m40` 写法 `db_core.py:652-670`）。
- **注册**：`db_core.py:1099` 后加 `(47, "tenant_isolation", _m47_tenant_isolation)`。
- **可回滚**：新建 `scripts/migrate_tenant_v47.py`（照 `migrate_phone_encryption_v39.py`：`_ensure_db()` + `--rollback`）。
  注意 SQLite `DROP COLUMN` 需 ≥3.35，否则回滚只清值不删列（**待确认**目标版本）。
- **配套写入改造（否则隔离失效）**：§A 列出的 8 处写入路径全部补写 `tenant_id`。

## F. 测试与演示

新建 `tests/test_tenant_isolation.py`，fixture 照 `tests/test_issue_phone_encryption.py:17-28` 的 `_fresh_db`
（只换 `db_core._DB_PATH`，**不要**写 `config.DB_PATH`，否则"单跑过、全量挂"）；LLM/演示开关要 `monkeypatch` 钉姿态。

用例：`test_v47_adds_tenant_columns` / `test_v47_normalizes_district_to_community` /
`test_v47_backfills_from_user_community` / `test_v47_idempotent` /
`test_grid_issue_list_excludes_other_community` / `test_export_issues_excludes_other_community` /
`test_grid_consults_excludes_other_community` / `test_public_questions_excludes_other_community` /
`test_notice_all_scope_is_community_scoped` / `test_grid_elderly_medications_excludes_other_community` /
`test_batch_dispatch_rejects_foreign_issue` / `test_unknown_tenant_fails_closed`（社区为空 → **返回空集**，不许回落全量）/
`test_resident_self_scope_unchanged`（守住原有基线）。

**演示路径**：`demo_resident`（海淀小区）建含唯一串的工单 → `demo_resident_cy`（朝阳试点社区）
取工单列表与 CSV，断言看不到该串 → 反向再验一次。

⚠ **演示缺口**：`auth.py:61-75` 演示登录取 `list_users(role)` 的**第一个**账号，`role=grid` 永远是 `demo_grid`（海淀小区）
→ **朝阳没有网格员账号**，无法演示"朝阳网格员看不到海淀工单"。**B5 需先补一个朝阳 grid 演示账号。**

## G. 刻意不改

| 表/路径 | 行数 | 原因 |
|---|---|---|
| `notifications` | 14099 | 已按 `user_id` 严格过滤（`db_notifications.py:175/181`、`messages.py:19,32`），无跨用户读点；加列是纯写放大 |
| `knowledge_base` | 62 | 政策语料是**跨社区共享参考数据**，属地由 `applicable_area` + `utils/region.py` 处理；加租户会破坏"跨区兜底"语义 |
| `health_surveillance` | 39 | 全国/区域公开监测数据，非社区数据 |
| `weather_cache` | 4 | 缓存键已统一用 adcode，非租户数据 |
| `settings` | 2 | 租户级配置候选，本轮不改（**待确认**是否需要分键） |
| agent 内部表 | — | 靠 user_id/会话派生，本轮不铺 |

## H. 已知坑

1. **写路径不同步 = 隔离假成功**（最高风险，见 §A）。
2. **测试隔离**：只换 `db_core._DB_PATH`；`conftest.py:45-78` 的 `_isolate_db_path` 会在模块结束复位。
3. **种子数据**：`seed.py:317/405/626`、`live_generator.py:225` 灌种子无租户 → 朝阳居民会看到海淀工单。
4. **前端不能参与隔离决策**（服务端从 JWT 取）；改 `web/src` 后必须 `cd web && npm run build`。
5. **fail-closed**：社区为空必须返回空集。
6. **`DEFAULT_TENANT` 语义分裂**：一次定稿，否则新库回填值仍是区名。


---

# 附录：补充侦察（A1–A5，均已在代码/库上复核）

> 来源：第二轮只读侦察的补充报告 + 我的逐条复核（2026-09-24）。前文 §D 的查询点清单**必须**与本节合并使用。

## A1【最高优先级】缓存键不含租户 → 加了过滤也会被缓存击穿

`utils/cache.py` 是 `st.cache_data(ttl=15)` 包装，**缓存键只有业务参数、没有 tenant/community/user**：

| 位置 | 函数 | 谁在用 |
|---|---|---|
| `utils/cache.py:120-124` | `cached_notices_with_stats(...)` | **主线** `api_routes/notices.py:82,86`（grid 通知管理列表） |
| `utils/cache.py:113-117` | `cached_proposals_full(status, limit)` | Streamlit 备线 `ui/pages_grid/proposals_mgmt.py:40,127` |
| `utils/cache.py:128-131` | `cached_active_alerts()` | **主线** `api_routes/weather.py:91,92` |

只在 `get_notices_with_stats` / `get_proposals` 里加 `tenant_id=?` 而**不给缓存键加租户维度**，
A 社区的结果会以 15s TTL 命中给 B 社区 —— **隔离被静默绕过，且比不加过滤更难排查**。
`utils/cache.py:197-207` 的 `invalidate_*` 是进程内全量失效，同样无租户维度。
→ v47 配套清单**必含**此项，并写专门回归测试。

## A2【新泄露面】网格员的自然语言 Agent 工具层完全绕过 REST（已复核）

`agent/web_agent_service.py` 直接查库，前文 §D 原来只按 `api_routes` 列查询点，**漏了这个文件**：

| 位置 | 函数 | 未过滤调用 |
|---|---|---|
| `:180` | `_grid_todo` | `get_issues(limit=1000)` 全库计数 ✅已复核 |
| `:181` | `_grid_todo` | `list_consults(limit=1000)` 全库 ✅已复核 |
| `:182` | `_grid_todo` | `list_medication_reminders()` 全库（**老年敏感**）✅已复核 |
| `:183` | `_grid_todo` | `get_overdue_issues()` 全库 |
| `:193-195`、`:203` | `_grid_stats` | 全库统计 |
| `:220-221` | `_grid_search` | `get_issues(limit=1000)` 后按关键词过滤 → **把他社区工单标题念进 Agent 回复** |
| `:227` | `_grid_search` | `get_proposals(limit=1000)` |

**为什么这条特别要紧**：它不是列表页，而是**对话式回答**，泄漏内容直接出现在 Agent 回复文本里，
`ui_audit`/`mobile_audit` 这类页面审计抓不到。

## A3【无门禁 + 无租户维度】`/api/web/weather/alerts`（已复核）

- `api_routes/weather.py:89-92`：函数体只有 `return _ok(cached_active_alerts())`，**无 `_require_role`**、`request` 未使用。
- `weather_alerts` 实测列：`id, alert_id, alert_type, level, effective_time, expire_time, status, created_at`
  → **既无城市列也无 tenant_id**，是全局单例表 ⇒ 任何已登录居民可拿全平台预警。
- 前端 `web/src/api/index.js:82` 在**通用 `weather` 对象**里（居民端也在用），所以**不能简单加 grid 角色校验**，
  正确修法是按社区切分预警（B5 一起做）。
- 附带：`data/db_weather.py:571-580` 的 `detect_extreme_weather(city="")` 收参数但调
  `fetch_hefeng_warnings()` 时**未透传 city**，而后者在 `:543` 硬编码 `COMMUNITY_CITY_ID`
  → **各社区官方预警同源同城**，属地化在天气预警上没落地，建议单独立项。

## A4【隐蔽失效】通知"指定小区/楼栋"的范围校验从来不生效（已复核）

`data/db_notice.py:487-501` 用 `communities` / `buildings` 两张表做范围有效性校验，
而**这两张表在库里不存在**（实测 52 张表清单里没有）→ 校验被跳过（`:491-499` 先查 `sqlite_master` 再决定是否校验）。
**注意**：这不是写错，而是"表不存在即视为未启用范围管理"的**特性开关式写法**，只是开关永远是关的。
后果："指定小区"的值从不校验，写错小区名不报错，配合 `_notice_visible_to_user`（`:202-205`）的精确串比对
→ 静默变成"谁都不匹配的孤儿通知"。做租户隔离若依赖 `publish_scope`，必须先修它或改走 `tenant_id`。

## A5 其他已核实的跨租户扇出 / IDOR（补入 §D 与 §G）

| 位置 | 问题 |
|---|---|
| `data/db_policy.py:735-750` | 读 `notices`（无租户条件）后 `list_users(role="grid")` **给所有社区网格员发通知** |
| `data/db_notifications.py:41-42` | `broadcast_notification` 全库居民扇出 |
| `data/db_notifications.py:202-205` | `mark_read` 只按 `id`、**无 `user_id` 条件**（IDOR）；当前仅 Streamlit 调用，**待确认**后续是否被新路由复用 |
| `data/db_weather.py:984` | `_notify_managers` 向全平台 grid 广播 |
| `data/db_weather.py:1143` | `_notify_system_admin` 硬编码 `get_user_by_id(1)` |
| `data/db_notice.py:167` | `_scope_user_ids` 取全库居民/老人作已读率分母 → `get_notice_read_stats`（`:728`）统计口径跨社区 |
| `api_routes/weather.py:41-42` | `exception_log` 路由内联全表 SQL（grid） |
| `api_routes/weather.py:102-110` | "确认任务"取 `list_check_tasks(limit=1000)` 后**在内存按 id 匹配** → 任意 grid 可确认任意社区任务（写入越权） |
| `data/db_notifications.py:277-301` | `get_activity_feed` / `get_activity_summary` 全表 `activity_log`（支持"`activity_log` 需处理"的判断） |

## 结论：不变项与修正项

- **不变**：租户键用**社区名**；v41"只回填不写入"的诊断；v47 迁移设计；pytest 方案；
  `notifications`（14099 行）不改的判断（`messages.py:18-19,32`、`db_notifications.py:174-185` 确认按 `user_id` 严格过滤）。
- **修正**：查询面从"`api_routes`"扩到"`api_routes` + **agent 工具层** + 缓存层"；
  `weather_alerts` 从"非租户数据"改判为**全局单例表、真泄露**（`weather_cache` 仍是 adcode 缓存键、不属租户数据）。
- **待确认（未编）**：① 是否有其他调用点传入外部可控的 `city`；② `mark_read` 是否会被新 FastAPI 路由复用；
  ③ `communities`/`buildings` 是否由仓库外脚本建表（本仓 grep 为 0，故校验确实不生效）。
