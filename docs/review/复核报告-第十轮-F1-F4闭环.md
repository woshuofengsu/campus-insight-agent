# 复核报告 · 第十轮 —— 第九轮 F1–F4 闭环验证

> 方式：不看提交说明，对当前代码与实跑结果独立复核（重跑 pytest / ruff / 54 页 ui_audit / 21 页 mobile_audit，逐行读四个修复点）
> 对象提交：`9890049`（审计扩面 54 页视口 + F1–F4 + 自查三项）
> 时间：2026-09-13

## 总结论：四个问题全部正确闭环，且修得比建议更彻底；无新增问题。可以冻结。

你这轮不止改了 F1–F4，还顺手做了三件我没要求但明显加分的事（审计从 26 扩到 54 页视口、jsErrors 计入 HIGH、新增 net_probe 处理校园 DNS）。我全部独立复核，结果为真。

## 一、F1–F4 逐项核验（全部通过）

| 项 | 我的建议 | 实际改法（逐行核实） | 结论 |
|---|---|---|---|
| **F1** 公网脚本局域网暴露 | 默认绑回环、加 `--lan` | `serve_public.py:135 start_server(lan=False)`、`:148 host="0.0.0.0" if lan else "127.0.0.1"`、`:400 --lan 开关`、`:416` 传入 args.lan，并打印当前绑定范围；cloudflared 走 127.0.0.1 不受影响 | ✅ 正确闭环，参数贯通 |
| **F2** 提案详情误弹错误 toast | 按后端 5 状态白名单门控 comments | `ProposalDetail.vue:19 CAN_DISCUSS` 与 `proposals.py:230-232` **逐字一致**；`:26` 不可议论时直接给 `[]`、**不发请求**；`:126` 整块议论卡 `v-if="canDiscuss()"`（含 is_public 判断，连输入框都不显示） | ✅ 前后端口径完全对齐 |
| **F3** 暗色首帧 / 假对比度 | mount 前先落暗色类 | `main.js` 在 `createApp` 前 try/catch 读 localStorage 给 body 加 dark；**并更正了我的根因判断**：另有一处写死 #047857 不读变量，已一并令牌化（双保险） | ✅ 采纳且更严谨 |
| **F4** 改完前端忘 build | 审计加 dist 新鲜度闸 | `ui_audit.py:312 _dist_stale_check()`：比较 `web/src` 最新 mtime 与 `dist/index.html`，落后直接红字退出；mobile_audit 同步加；约定写进 AGENTS/交付说明 | ✅ 工具层面根治 |

> 关于 F3 的根因更正我接受：我上轮把 2.96:1 主要归因于采样时序，你定位到还有一处真写死色值。你同时做了"挂载前定类 + 令牌化"，是更稳的解法。

## 二、独立实跑门禁（全部为真）

| 检查 | 结果 |
|---|---|
| `pytest tests/ -q` | **581 passed, 1 skipped, 3 deselected, 7 subtests，0 warnings（416s）** |
| `ruff check .` | **All checks passed** |
| `ui_audit.py`（扩面后 **54 个页面/视口**，覆盖全部 34 路由） | **无 HIGH（0 处）**；逐页解析 JSON：54 页、**jsErrors=0**、对比度/字号/溢出/热区/断图/残缺文本全部为空 |
| `mobile_audit.py`（21 页真机口径） | **全部通过** |
| F2 反向验证 | 审计用真实 id #189（正是上轮 400 的"不可议论"提案）进详情页，**jsErrors 现计入 HIGH 且为 0** → 前端确实不再发那条 400；后端拦截仍保留（纵深防御没被拆掉） |
| dist 新鲜度 | dist 19:55 新于最新源码 19:54，测的是当前包 |
| 工作区 | `git status` 干净，评审未改任何源码、无临时脚本残留 |

## 三、加分的三项自查（独立看过，无副作用）

1. **jsErrors 计入 HIGH**：此前控制台报错只打印不计违规，所以 F2 那种 400 旧审计抓不到；现在计入后 54 页为 0，等于把"页面一加载就报错"这类问题纳入了硬门禁——这是审计能力的实质增强。
2. **`net_probe.py`（103 行）**：校园网对新隧道域名 NXDOMAIN 的兜底——UDP/53 直问公共 DNS + 仅在本进程内改写 `getaddrinfo` + TLS 仍用原域名做 SNI。我读过：**只读诊断、不碰系统配置、无 subprocess/文件删除，安全**。
3. **审计扩面立刻抓出并修掉 6 处**（Select 占位符、暗色状态标签、字数统计、居民大数字、通知正文、报修数字），并把"26 页"过时表述用 check_claims 在 12 份材料里统一成 34 页/54 视口——数字门禁的自我一致性保持得很好。

## 四、还剩什么（都不阻塞，无需现在动）

- 仅存两条**观察项**（上轮 O1/O2，非缺陷）：PWA 无 service worker（可安装、不支持离线，答辩别说"离线可用"）；581 里有 6 条供消融报告的非断言冒烟项（关键指标另有强断言兜底）。
- 长期生产债不变（Redis/PG/多租户/真实街道试点），比赛期不动。

## 最终结论

**到这一版，连续九轮审下来的所有 P1/P2/P3 已全部清零，门禁（581 测试 + ruff + 54 视口视觉 + 21 页移动 + 9 项自检 + 11 项真实 HTTP 验收 + 数字门禁）互相咬合、且都能独立复现。竞赛完成度 9.5，没有再改代码的必要了。**

接下来只做三件非代码的事：
1. `git tag v1.0` 冻结；
2. 用 serve_public 出的公网链接拿**真手机**扫一遍三端主流程并录 30 秒素材；
3. 全部精力转答辩演练（上轮报告给了 5 个本轮新增问题的答法）。
