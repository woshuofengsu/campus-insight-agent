# Product

<!-- impeccable:product-schema 1 -->

> 本文件描述**当前形态**（FastAPI + Vue3 三端 + 9 智能体角色）。早期 Streamlit 单页形态见 `docs/TECHNICAL.md`（历史实现）。

## Platform

web（FastAPI 主服务 `:8000` 托管 Vue3 三端；另有 `api.py` 扣子插件入口 `:18800`、`app.py` Streamlit 备线 `:8501`，均非主线）

## Users

- **居民（Resident）**：看社区动态（天气、通知、政策问答）、一句话上报诉求并追踪闭环、参与提案与议事、查看治理透明度（14 页）
- **网格员（Grid Worker）**：工作台 KPI 与 SLA、工单流转、提案办理、内容发布（通知/知识库含"适用地区"）、Agent 留痕查询、治理大屏（9 页）
- **老年人（Elderly）**：大字大按钮的关怀版——预置短语、语音报修与播报、用药提醒、紧急联系人、一键 SOS（8 页）

## Product Purpose

社区先知（CommunityInsight）是一个社区基层治理平台（演示落地「海淀小区」，第二演示社区「朝阳试点社区」），主打接诉即办。居民自下而上发现并上报问题，**9 个声明式智能体角色**通过黑板消息队列协商处理（分类、派单、政策检索、天气联动、通知），网格员自上而下统一办理；每轮输出都过 **Verifier（幻觉/隐私/诊断拦截）+ Arbiter（冲突仲裁留痕）** 双层防线。闭环：感知 → 上报 → 协商 → 处理 → 反馈。

## Positioning

**不是聊天机器人，也不是"多智能体"营销词**。差异化在三处：① 多智能体是**真协商**（角色间消息往返 + 执行链可投屏 + 仲裁落库）；② 安全与责任链是真的（**五类情形一律转人工并给处理包编号**，健康不做诊断）；③ 质量是**可复算的数字**（681 项可运行用例、12 项端到端、54 个页面视口审计、检索金标 hit@1 100%、故障注入 8/8）。此外**属地化（一区一策）不采定位**：政策按行政区划本地优先、全国兜底，且属地**只改排序、不改能不能自动回答**。

## Operating Context

- 老旧社区（海淀小区）日常运营场景；第二演示社区用于属地化对比
- 比赛演示环境（5 分钟内展示完整闭环 + 技术亮点）
- 评委背景：技术 + 产品 + 街道/社区实务
- **演示数据为程序生成的种子数据（虚构），不代表真实治理成效**；生产关闭 `DEMO_MODE` / `DEMO_AUTO_WORKER`

## Capabilities and Constraints

**能力：**
- 三端分离（居民 14 页 / 网格员 9 页 / 老年 8 页），设计令牌体系 + 暗色达标 + 移动端适配
- 9 个声明式角色 + 黑板消息队列 + 跨角色链式协商（含循环防护）+ 双层防线 + 全程留痕
- 16 个工具（`pkgutil` 自动发现 + 按角色裁剪）
- 知识库 62 条（61 已发布、55 条可溯源公开政策摘要、43 条带适用地区）+ 混合检索（词法/方言扩展 + 向量 + RRF）
- 轻量知识图谱（89 实体 / 202 关系 / 772 提及）：按实体反查历史工单与政策
- 属地化：政策属地优先（`utils/region.py` 唯一入口）、天气按社区 adcode、三端口径一致、**不采 GPS**
- 数据安全：手机号 AES-256-GCM 加密落库（8 表明文 0）、展示脱敏、审计留痕、注销级联；生产姿态密钥 fail-closed
- 成本可计量：`llm_usage` 逐笔记账（**单次均价 ¥0.0002**（2026-09-14 快照：57 次调用合计 ¥0.0139）；规则优先，四个 LLM 开关可一键关；`python scripts/llm_cost.py` 可复核）
- 治理大屏（动画 KPI + 趋势 + 关怀指标）、WebSocket 实时通知、PWA 可添加到主屏幕

**约束：**
- **单进程形态**：黑板 / 限流 / WebSocket 均在进程内，多 worker 需 Redis（见 `docs/scaling.md`）
- SQLite（WAL，schema v46）；PostgreSQL 迁移路径已写、比赛期不做大重构
- **无 service worker**：不宣称离线能力，业务强依赖后端实时数据
- 多租户仅预留 `tenant_id`，查询层未隔离
- DeepSeek（对话）+ 百炼 text-embedding-v3（向量）双模型依赖
- **无真实试点、未做非 Chromium 真机验证**（见交付说明"已知边界"）

**未决定：**
- 是否接入真实接诉即办/12345 系统（当前仅预留适配器框架）
- 是否上云（当前演示用本机 + 临时 HTTPS 隧道）

## Brand Commitments

- 名称：社区先知 · CommunityInsight
- 口号：知报议督（感知 · 上报 · 协商 · 监督）
- 主色调：设计令牌体系（indigo/purple 起家，现统一由 `web/src/style.css` 令牌驱动，亮暗成对）
- 体验目标：专业严谨（企业级可靠性）+ 邻里温度（社区生活气息），两者同等重要
- 风格参考：Linear 的工具感 + 中国老旧社区的美学元素
- 语言：简体中文

## Evidence on Hand

- 可运行主服务（FastAPI，130 条路由）+ Vue3 三端构建产物（`web/dist`，不入 git）
- 681 项可运行用例（680 通过 / 1 需外部服务跳过）、54 个页面视口 UI 审计、21 页移动端审计、12 项端到端验收
- 9 个角色 / 16 个工具 / 50 张业务表 / 46 个版本化迁移（schema v46）
- 62 条知识库条目（含 3 条属地政策：区级/社区级/市级）+ 48 条检索金标
- 7 段答辩兜底录屏（`.recordings/`，含属地化两社区对比）与录屏脚本
- `docs/competition/`：提交版创意说明书、技术实现报告、最终版交付说明、答辩问答手册、演示脚本、提词器

## Product Principles

1. **治理闭环而非聊天**：每个功能服务于「发现→上报→协商→处理→反馈」闭环，不堆砌技术噱头
2. **技术是手段不是目的**：执行链可视化，让评委看到"谁做了什么、谁拦了什么"，而不是黑盒
3. **三端真正分工**：网格端效率优先、居民端易用优先、**老年端独立重做**（不是把居民端放大）
4. **人机协同**：AI 做预处理与减负，**关键节点人确认**；涉及安全/费用/政策依据一律转人工
5. **不越界、不采集、不静默失败**：不做诊断、不采定位、答不了就转人工并给编号
6. **比赛级打磨**：5 分钟演示内呈现完整闭环 + 技术亮点 + 视觉品质，且每个数字都能现场复算

## Accessibility & Inclusion

- 亮色/暗色双主题支持（语义色亮暗成对令牌，禁止内联写死 hex）
- 老年端：字号 ≥20px、热区 ≥72px、只保留 SOS 一个呼吸动效、全站尊重 `prefers-reduced-motion`
- 响应式布局（320/390/1366/1440/1920 五档），移动端底栏/横屏遮罩/输入框聚焦放大均已审计
- 中文字体优化（PingFang SC / Microsoft YaHei / Noto Sans SC）
- 语音为增强而非唯一路径：**不依赖语音也有打字 + 预置短语保底**
