# 开发约定

自己记的规矩，以后每次动这个项目的代码都照这个来。

## 改代码的铁律

只动你要改的那一处，别的碰都别碰。下面这些是红线：

- 代码逻辑（变量名、函数名、判断、循环、返回值）
- SQL / CSS / 正则 / JSON 键名
- 给用户看的文案（st.info、st.warning、st.error 里的字）
- 给 LLM 看的工具描述（@tool 下面的 docstring）

## 注释怎么写

- 全写中文，口语化，像自己随手记的
- 不写英文，不写「注意」「特别说明」「本函数用于」这种文档腔
- 不搞 `# ── 分节 ──` 这种装饰线
- 废话注释直接删（比如 `i += 1  # 自增`）
- 只留「为什么这么做」和「这里有个坑」
- 技术名词保留英文：LLM、SQL、RAG、SLA、OfflineAgent，翻成中文反而看不懂

## 改完必做

1. `python -m pytest -q`（**基线以 `python scripts/check_claims.py` 为准**：当前可运行 621 项 = 620 通过 + 1 需外部服务跳过，必须全绿）
2. `python scripts/demo_preflight.py --fast`（9 项自检：schema / 手机号加密覆盖 / 账号 / 服务身份…）+ `ruff check .`
3. `python -m py_compile` 改过的文件
4. `git add -A && git commit -m "一句话说改了什么"`
5. `git push`

## 别踩的雷

- `.env` 里的真实密钥绝不提交（已被 gitignore）
- 密码盐 `campus-insight-salt-2026` 不改
- `ui/components.py` 的 CAT_LABEL 是旧校园分类的兼容层，保留
- `db_core.py` 里的旧表名（campus_issues / student_id）迁移逻辑保留，别删
